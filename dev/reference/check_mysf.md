# Check `sf` object

Check an `sf` object for its class, `$geometry` column, and geometry
class.

## Usage

``` r
check_mysf(x)
```

## Arguments

- x:

  an `sf` object

## Value

NULL; returns stop error if `x` does not match class and/or column
expectations

## Author

Eva Marques
