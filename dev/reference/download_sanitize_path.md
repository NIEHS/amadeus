# Sanitize directory

Append forward slash to end of `directory` if it does not already end
with one.

## Usage

``` r
download_sanitize_path(directory)
```

## Arguments

- directory:

  character(1). Path

## Value

character ending with a forward slash.
