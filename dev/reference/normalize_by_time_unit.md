# Normalize `.by_time` time-unit aliases

Internal helper that maps singular/plural `.by_time` tokens to canonical
units.

## Usage

``` r
normalize_by_time_unit(unit)
```

## Arguments

- unit:

  character(1). Time-unit alias.

## Value

character(1). Canonical unit.

## Author

Insang Song
