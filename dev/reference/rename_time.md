# Rename `$time`

Rename the `$time` column of an `sftime` object.

## Usage

``` r
rename_time(x, newname)
```

## Arguments

- x:

  an `sftime` object

- newname:

  character for new time column name

## Value

an `sftime` object

## Author

Eva Marques
