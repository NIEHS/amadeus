% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_covariates.R
\name{calculate_xis}
\alias{calculate_xis}
\title{Calculate XIS covariates}
\usage{
calculate_xis(
  from,
  locs,
  locs_id = "site_id",
  radius = 0,
  fun = "mean",
  weights = NULL,
  .by_time = NULL,
  geom = FALSE,
  max_cells = 1e+08,
  ...
)
}
\arguments{
\item{from}{SpatRaster. Output of \code{process_xis()}.}

\item{locs}{data.frame, sf, or SpatVector. Unique locations containing
\code{locs_id}. A data.frame must contain \code{lon} and \code{lat}.}

\item{locs_id}{character(1). Name of the unique location identifier.}

\item{radius}{numeric(1). Circular buffer radius in metres. Use \code{0}
(default) for point extraction.}

\item{fun}{character(1) or function. Function used to summarize raster cells
within buffered or polygon locations. Default is \code{"mean"}.}

\item{weights}{NULL, SpatRaster, polygon SpatVector/sf, or file path.
Optional weighting surface for buffered extraction.}

\item{.by_time}{NULL or character(1). Optional temporal grouping unit.}

\item{geom}{FALSE, \code{"sf"}, or \code{"terra"}. Return geometry in the
requested form.}

\item{max_cells}{integer(1). Maximum cells read at once during exact
extraction.}

\item{...}{Placeholders.}
}
\value{
A data.frame, sf object, or SpatVector containing one row per
location and XIS time layer, with columns for the location identifier,
time, and extracted value.
}
\description{
Extract multitemporal XIS prediction values at input locations. A nonzero
\code{radius} creates circular buffers around point locations and summarizes
intersecting raster cells with \code{fun}.
}
\examples{
\dontrun{
calculate_xis(
  from = xis,
  locs = locations,
  locs_id = "site_id",
  radius = 1000
)
}
}
\seealso{
\code{\link{download_xis}}, \code{\link{process_xis}}
}
\author{
Insang Song
}
