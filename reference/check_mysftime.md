# Check `sftime` object

Check an `sftime` object for its class, `$time` column, `$geometry`
column, and geometry class.

## Usage

``` r
check_mysftime(x)
```

## Arguments

- x:

  an `sftime` object

## Value

NULL; returns stop error if `x` does not match class and column
expectations

## Author

Eva Marques
