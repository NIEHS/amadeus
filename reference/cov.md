# Calculate code coverage of the `beethoven` package with the `container.sif` container.

Calculate code coverage of the `beethoven` package with the
`container.sif` container.

## Usage

``` r
cov()
```

## Value

NULL; Prints the output of the code coverage.

## See also

[`covr::package_coverage()`](http://covr.r-lib.org/reference/package_coverage.md);
[`covr::coverage_to_list()`](http://covr.r-lib.org/reference/coverage_to_list.md)
