# Open interactive session with `container.sif` container.

Open interactive session with `container.sif` container.

## Usage

``` r
interactive(dir = ".")
```

## Arguments

- dir:

  character(1). Directory with `interactive.sh`
